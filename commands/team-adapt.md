# team-adapt

Follow the **team-adapt** skill. As lead, call **`team_adapt`** (MCP), **`npm run team:adapt`** (AGENTS.md only), or write profiles manually:

- **Chat overrides win** — pass `workers` for any role the user described in this conversation.
- **Otherwise** — personas come from repo **`AGENTS.md`** (auto-parsed).
- Writes `team/project.json` + `knowledge/project.md`. Does not edit or replace `AGENTS.md`.
