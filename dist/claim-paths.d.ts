/** Normalize and validate repo-relative claim paths (no traversal or absolutes). */
export declare function normalizeClaimPath(raw: string): string;
export declare function normalizeClaimPaths(paths: string[]): string[];
