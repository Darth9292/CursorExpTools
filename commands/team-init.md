---
name: team-init
description: Scaffold team/ files and start the agent-team bus for this project
---

Follow the **team-init** skill. Scaffold `team/` in this workspace, ensure the HTTP bus on port 7391 is healthy, then print the easy-start leftover: prefer `/team-start` in the lead chat; Duplicate Workspace; if Allow, click **Allow always** (localhost / agent-team MCP — [knowledge/approvals.md](../knowledge/approvals.md)); `/team-worker` (orders watcher, not a 1m `/loop`); optional `/team-worker C`; optional fourth Duplicate Workspace, Allow always, `/team-worker D`. Do not paste a `/loop` prompt.
