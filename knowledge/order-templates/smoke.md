# Order template: SMOKE

**Title:** SMOKE: (one artifact)

**Brief:**
- One small deliverable proving wake path works (e.g. `knowledge/smoke-<id>.md` one line).
- Use `team_report` when done; leave watcher running.

**doneWhen:** artifact exists

**claim:** `knowledge/smoke-<id>.md`

**mode:** `parallel`
