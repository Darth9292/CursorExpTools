# cursor-agent-team (plugin)

Dogfood and ship the **cursor-agent-team** Cursor plugin: multi-window lead **A** + workers **B–D**, shared `team/` board, localhost MCP bus, watcher wake (no timed `/loop`).

## Agent B — Runtime & MCP

- **Title:** Runtime & MCP engineer
- **Focus:** `server/`, store, MCP tools, hooks (`watch-orders`, `start-watcher`), tests, `dist/` and bus lifecycle

### C - Plugin UX & docs

- **Title:** Plugin UX & docs
- **Focus:** skills, commands, rules, README, `/team-adapt` messaging, anti-loop docs

- D: Ops & integration — SMOKE, `link-local`, `pack:check`, `knowledge/`, marketplace prep, `.gitignore` and board hygiene
